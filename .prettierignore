# Generated by Ermine and synced with `npm run ermine:css`. Reformatting it makes the next
# sync produce a spurious diff and breaks the "do not edit" contract in its header.
src/styles/grammar/ermine.generated.css

# CSS is left alone. Prettier rewrites attribute selectors and quoted values --
# [aria-checked="true"] to [aria-checked='true'], input[type=number] to input[type='number']
# -- which is equivalent CSS but changes the selector text the adoption ledger keys its rows
# on, and diverges from the double quotes Ermine's own emitter writes. The JSX layout this
# config exists for is a TypeScript concern.
*.css

dist
node_modules
